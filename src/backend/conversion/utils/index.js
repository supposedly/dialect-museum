const parseWord = require(`./parseWord`);
const misc = require(`./misc`);
const syllables = require(`./syllables`);
const phonemeProperties = require(`./phoneme-properties`);

module.exports = {
  parseWord,
  misc,
  syllables,
  phonemeProperties
};
